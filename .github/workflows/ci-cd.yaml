name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DOCKERHUB_USERNAME: sodapopin

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push blog-api
        uses: docker/build-push-action@v4
        with:
          context: ./blog-api
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/blog-api:latest

      - name: Build and push auth
        uses: docker/build-push-action@v4
        with:
          context: ./auth
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/auth:latest

      - name: Build and push analytics
        uses: docker/build-push-action@v4
        with:
          context: ./analytics
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/analytics:latest      - name: Build and push apache
        uses: docker/build-push-action@v4
        with:
          context: ./apache
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/apache-reverse-proxy:latest
